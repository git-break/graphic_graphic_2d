diff a/rosen/modules/render_service_base/include/pipeline/rs_surface_handler.h b/rosen/modules/render_service_base/include/pipeline/rs_surface_handler.h	(rejected hunks)
@@ -227,10 +227,10 @@ public:
     }
 #endif
 
-    void ResetPreBuffer()
+    void ResetPreBuffer(bool needBufferDeleteCb = true)
     {
         std::lock_guard<std::mutex> lock(mutex_);
-        preBuffer_.Reset();
+        preBuffer_.Reset(needBufferDeleteCb);
     }
 
     int32_t GetAvailableBufferCount() const
