diff a/rosen/test/render_service/render_service/fuzztest/rsrenderserviceconnection010_fuzzer/rsrenderserviceconnection010_fuzzer.cpp b/rosen/test/render_service/render_service/fuzztest/rsrenderserviceconnection010_fuzzer/rsrenderserviceconnection010_fuzzer.cpp	(rejected hunks)
@@ -244,6 +246,36 @@ bool DoSetVirtualScreenAutoRotation()
     toServiceConnectionStub_->OnRemoteRequest(code, dataP, reply, option);
     return true;
 }
+
+void DoGetScreenVCPFeature()
+{
+    uint32_t code = static_cast<uint32_t>(RSIClientToServiceConnectionInterfaceCode::GET_SCREEN_VCP_FEATURE);
+    MessageOption option;
+    MessageParcel dataParcel;
+    MessageParcel replyParcel;
+    ScreenId id = GetData<uint64_t>();
+    uint8_t vcpCode = GetData<uint8_t>();
+    dataParcel.WriteInterfaceToken(RSIClientToServiceConnection::GetDescriptor());
+    dataParcel.WriteUint64(id);
+    dataParcel.WriteUint8(vcpCode);
+    toServiceConnectionStub_->OnRemoteRequest(code, dataParcel, replyParcel, option);
+}
+
+void DoSetScreenVCPFeature()
+{
+    uint32_t code = static_cast<uint32_t>(RSIClientToServiceConnectionInterfaceCode::SET_SCREEN_VCP_FEATURE);
+    MessageOption option;
+    MessageParcel dataParcel;
+    MessageParcel replyParcel;
+    ScreenId id = GetData<uint64_t>();
+    uint8_t vcpCode = GetData<uint8_t>();
+    uint16_t currentValue = GetData<uint16_t>();
+    dataParcel.WriteInterfaceToken(RSIClientToServiceConnection::GetDescriptor());
+    dataParcel.WriteUint64(id);
+    dataParcel.WriteUint8(vcpCode);
+    dataParcel.WriteUint16(currentValue);
+    toServiceConnectionStub_->OnRemoteRequest(code, dataParcel, replyParcel, option);
+}
 } // namespace Rosen
 } // namespace OHOS
 
